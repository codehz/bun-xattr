{
  "name": "bun-xattr",
  "module": "index.ts",
  "type": "module",
  "version": "0.0.3",
  "description": "Bun wrapper for getting, setting, removing and listing extended attributes on files.",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
